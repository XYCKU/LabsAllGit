#include <iostream>
#include <cstdio>
#include <cmath>

using namespace std;

int main()
{
	double a = -0.8, b = 0.9, h = 0.1, EPS = 0.0001, delta = 1;

	for (double x = a; x < b + h / 2; x += h) {

		while (fabs(delta) > EPS) {

		}
	}

	return 0;
}
